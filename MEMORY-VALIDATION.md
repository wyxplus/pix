# 记忆功能验收记录（2026-09-20）

当前结论：独立 160 场景已执行，152 通过、5 失败、3 个非本机平台项待验证，完整发布验收尚未通过。LongMemEval-S 500 题真实模型四组对照已于 2026-09-22 在新主机上完整跑通一轮（glm-5.3-flash 作答 / qwen3.8-flash 替代裁判，2,000/2,000 作答与评分）：pix-memory 与无历史基线持平，归因于该模型作为记忆学习模型的证据引用保真度不足，尚不能据此评价记忆系统设计上限。用户已明确授权不限制总预算。

| 验证项                                | 本轮结果                             | 证据及边界                                                                                                                                            |
| ------------------------------------- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 独立 160 场景                         | 152 通过、5 失败、3 待验证           | 本地逐场景报告（`apps/desktop/scripts/eval/fixtures/acceptance-160-real-model-macos-arm64.json`，含本机指纹，未入库），与单元测试数量分开统计         |
| 80 个记忆场景                         | 75 通过、5 失败；真实模型调用 128 次 | 40 开发场景 + 40 保留场景；是独立编写的产品语料，不是第三方盲测                                                                                       |
| 40 个存储/导出场景                    | 37 通过、3 等待对应平台              | 覆盖归档、附件、遗忘、迁移和恢复                                                                                                                      |
| 40 个原生迁出场景                     | 40 通过                              | Claude Code 2.1.87、Codex CLI 0.155.0-alpha.9.2；含实际终端恢复列表及历史预览；不代表桌面客户端或 IDE 兼容                                            |
| macOS ARM64 DMG                       | 四项安装资源检查通过                 | 本地安装报告（`apps/desktop/scripts/eval/fixtures/installed-macos-arm64.json`，未入库）；临时签名，未公证；另已完成原生界面开关、重启持久化及遗忘检查 |
| macOS Intel / Windows x64 / Linux x64 | 尚未运行                             | [四平台 CI 工作流](.github/workflows/memory-acceptance.yml)已改为手动触发并可选 ref，可在 `main` 或已发布的 `v0.8.0` 标签上直接运行                   |
| LongMemEval-S 500 题                  | v2 修复协议完成：pix-memory 32.0%    | 2,000/2,000 作答与评分；v1 缺陷协议结果归档作对照；详见「归因」与「修复与协议 v2」章节                                                                |

`apps/desktop/scripts/eval/fixtures/` 下的报告都是本机运行产物，记录了运行机器、源码/产物哈希、时间戳或真实模型输出，因此全部只保留在本地并由 `.gitignore` 排除，仓库只保留生成它们的脚本、工作流和命令。上表中的结果可以在没有这些文件时独立核对，重跑命令见下文。

## 已发现并修复的问题

真实安装包的数据目录迁移曾拒绝 Pix 自己创建的 Python venv 符号链接。修复允许迁移托管运行时内部的链接，并改写 venv、pip 和 npm 启动脚本中的旧路径。指向托管目录之外的链接仍会停止迁移并保留源数据。

安装验证执行了迁移后的 Python 和 pip，成功导入预先放入 venv 的模块，确认长期记忆、遗忘状态和会话保留，新数据写入新目录，旧数据库未发生变化。

源码指纹排除了生成的 `node_modules` 和测试缓存，安装包携带构建时指纹。验收拒绝使用其他源码版本的安装报告；续跑还校验数据集、语料、评测协议、模型及预算设置。67 项相关单元测试以及新一轮 6 项评测工具回归通过，417 个文件的类型、格式与静态检查通过。

## 失败场景及复核

- `memory.topology.move-keeps-identity`
- `memory.topology.rename-folder`
- `memory.topology.absolute-path-external`

三个场景都实际移动了项目目录，失败原因相同：项目身份使用规范路径或 Git common directory 路径，目录移动后会被识别为新项目，旧记忆还在原项目下。它们没有用固定 ID 模拟成功，也没有从验收分母中删除。当前可通过已有导出/导入流程显式映射到新项目；自动保持关联仍需实现。

另外两项原始失败保留在分母和报告中，未通过事后改规则提高通过率：

- `memory.scope.docs-locale-exception`：答案为“Alpha 发布的 API 文档必须使用英语”，语义正确，断言只接受英文字符串 `English`，属于多语言断言误判。
- `memory.quality.rejected-suggestion`：答案和记忆都明确表示 DB_GARNET 已被拒绝、数据库尚未选定；测试却要求不能出现该名称且活跃记忆为零。这与保留明确否决决策/当前状态的能力有冲突，需要修订下一版语料的需求定义。

本地独立复核记录（`apps/desktop/scripts/eval/fixtures/acceptance-160-real-model-triage.json`，未入库）与原始运行结果分开保存。

本地 macOS 原生 UI 记录（`apps/desktop/scripts/eval/fixtures/native-macos-arm64-memory-ui.json`，未入库）绑定同一安装包及源码指纹：默认关闭、四种开关组合、记住、关闭后的编辑禁用、原生进程重启后的开关与内容持久化、关闭时遗忘均已实际操作。测试使用隔离目录，没有发起模型回答。直接运行可执行文件遇到 Tauri `unknown path`，改用系统标准 Launch Services 启动成功；安装资源测试不能替代这项 UI 观察。

## 500 题真实评测的运行配置

首轮 CPA / `gpt-5.6-sol` 与 `gpt-5.5` 因服务返回 `429 model_cooldown`（约 141 小时）停止，保留 247 份有效答案、243 份评分和所有失败/不确定调用记录。用户要求继续使用 CPA 后，确认 `gpt-6-astra` 通过约 11 万 Token 的长上下文实测，裁判 `gpt-5.6-terra` 也验证可用，已建立全新的独立运行。不同模型的答案不合并计分。当前接口没有官方裁判 `gpt-4o-2024-08-06`，因此两轮均明确标记为替代裁判评分，不能与官方裁判的论文分数等同。裁判提示通过 SHA-256 固定的官方 `evaluate_qa.py` 中 `get_anscheck_prompt` 提取，保留类别和拒答规则；输出上限为 128 Token，温度 0。

用户已授权无需考虑总预算，运行器使用显式 `--unlimited-budget`。CPA 配置没有可靠单价，费用字段记录 `null`，保留实际输入/输出 Token 和逐调用日志，不把零配置价格当成免费。

四组对照共有 2,000 次作答、2,000 次裁判评分；数据包含 23,867 段会话，最多形成 23,487 个抽取批次和同等数量的沉淀调用。实际数量受存储预算和抽取结果影响。每题仍使用产品支持的每日整理 Token 上限 1,000,000，超长用户消息和达到该上限后的来源会被跳过并计数。解除的是评测总费用限制，不是更改产品记忆策略。

新一轮 32 并发触发约 1 秒短期限流，程序自动停止；连通性复核后降为 8 并发，裁判降为 2 并发，复用已经完成的 96 份基线答案。断点续跑复用已经完成的答案，重建被中断的记忆组，之前的调用仍在日志中。裁判持续读取新答案，接口失败自动补评，已评分答案不会重复计费。两轮之间未修改产品源码或提取/沉淀提示。第二轮修复了评测器：按时间稳定排序会话；对临时错误有限重试；在凭据冷却、额度耗尽或持续服务错误时停止，避免继续产生大量无效调用。第一轮协议与原始报告另行归档。

本地实时进度（`artifacts/memory-evaluation-cpa-astra-20260920/PROGRESS.md`，`artifacts/` 未入库）由运行监督程序更新，最终报告会写入同目录。程序只在当前评测中运行，不是周期任务；短期限流至多三轮退避/降并发补跑，长时间冷却即停止并保存部分证据。

`report.mjs` 分别提供全量/部分覆盖、四组准确率及 95% Wilson 区间、题型、拒答、配对提升、检索证据召回、延迟、跳过来源和抽取失败数。`full500AlternativeScored` 与官方 `full500Scored` 分开；只有完成 500 × 4 的有效答案与评分才报告全量准确率。

## 2026-09-22 完整一轮（Windows x64 主机，glm-5.3-flash / qwen3.8-flash）

主机切换到 Windows x64 后，接口从 CPA 切换为本机配置的两个模型，独立重跑了一轮全量评测：**glm-5.3-flash 作答，qwen3.8-flash 替代裁判**。2,000/2,000 作答与 2,000/2,000 评分全部完成，无缺漏。证据目录 `artifacts/memory-evaluation-glm-qwen-20260922/`（`artifacts/` 未入库），含 `RESULTS.md`、`final-scores.json`、逐题答案与裁判标签、逐调用日志。原 CPA / gpt-6-astra 部分运行按协议不与本轮合并。

| 对照组             | 有效答案 | 已评分 | QA 准确率 | 95% Wilson 区间 | p50 延迟 |
| ------------------ | -------- | ------ | --------- | --------------- | -------- |
| none（无历史）     | 500      | 500    | 9.0%      | 6.8–11.8%       | 3s       |
| recent（最近两段） | 500      | 500    | 11.6%     | 9.1–14.7%       | 3s       |
| full（完整历史）   | 500      | 500    | 82.6%     | 79.0–85.7%      | 12s      |
| pix-memory         | 500      | 500    | 10.0%     | 7.7–12.9%       | 191s     |

裁判为 qwen3.8-flash（官方 `evaluate_qa.py` 固定提示，SHA-256 校验，128 Token 上限，温度 0），属替代裁判评分，不能与官方 gpt-4o 论文分数等同。

**关键发现：pix-memory（10.0%）与 none 基线（9.0%）持平，而 full 历史达 82.6%。** 归因证据链：

- 抽取失败覆盖全部 500 题，共 10,546 次：产品要求逐字引用用户消息证据，该模型大量输出未通过严格校验。
- 检索证据召回平均 0.083，417/500 题为 0：答案上下文平均 1,819 字符（上限 6,000），其中几乎不含答题证据。
- 截断不是原因：28,510 次调用中 28,498 次正常停止，仅 1 次 length 截断；11 次上游瞬时错误（其中 1 题首轮失败，续跑已补齐）。
- 对照试点：qwen3.8-flash 抽取失败约 3 次/题，glm-5.3-flash 约 21 次/题，差距约 7 倍。失败指向该模型的证据引用保真度，而非检索或注入链路缺陷。

结论边界（后续归因已推翻部分判断，见下节）：本轮 pix-memory 低分的主因不是模型能力，而是**评测器伪造的 entryId 存在结构性冲突**，导致记忆库在学习阶段几乎无法积累；归因与修复详见下节。

### 归因：pix-memory 为何与基线持平（2026-09-22 复盘）

逐批捕获两个模型的抽取响应并分类失败原因，结论如下：

1. **评测器 entryId 结构性冲突（主因，评测器 bug）**：`longmemeval.mjs` 伪造 `entryId = entry-${会话内消息下标}`，跨会话必然重复；校验器按 entryId 匹配且要求恰好命中 1 条。实测 500 题共 6,331 个学习批次中 6,116 个（96.6%）存在 entryId 冲突——即使模型完全正确也会被整批拒绝。生产代码（`agent-host/index.ts`）使用全局唯一 entry id，不存在此问题，即 v1 协议没有复现生产条件。
2. **提示词契约脆弱**：提示词要求输出 entryId 但未说明必须原样复制。qwen 系统性输出 `sessionId:entryId` 前缀形式（永不匹配），glm 顺序重编号（撞冲突校验）。
3. **glm 用 Markdown 代码栅栏包裹 JSON（模型特定，但暴露真实产品问题）**：glm 77% 的抽取响应带 ```json 栅栏，生产代码裸 `JSON.parse` 不剥栅栏，异常即整批丢弃。诊断样本中 glm 77/77 批中 59 批因此失败。
4. **全有全无批次校验是放大器**：≤8 个候选中错 1 个，20 条消息的学习全部作废（生产同款行为，予以保留）。
5. 次要：单条用户消息超 4000 字符被跳过（528 条）。

因果链：entryId 冲突 + 输出不合规 → 抽取批次大量作废（glm 10,546 次）→ 记忆库近乎空 → 检索证据召回 0.083 → 答案上下文无答题事实 → pix-memory ≈ none。注入与检索链路本身正常（每题均送达约 1,819 字符上下文）。none/recent/full 三组基线不受影响，仍为有效模型能力数据。

### 修复与协议 v2（2026-09-22）

- `longmemeval.mjs`：entryId 改为每题全局唯一（`m{会话序号}-{消息下标}`），对齐生产唯一性语义。
- `packages/agent-runtime/src/memory-extraction.ts`：新增 `parseModelJsonArray`（容忍 Markdown 代码栅栏）；生产 `agent-host` 与评测器同步使用；抽取提示词明确“entryId 必须原样复制”。
- 新增单测 `memory-extraction.test.ts`（5 项通过）；`longmemeval.test.mjs` 夹具同步更新后通过；`pnpm check` 全绿。协议指纹变化：protocolSha256 `be4fffce…` → `5df54b20…`，extractionPromptSha256 `397951e0…` → `d092b7f1…`。
- 修复后同批样本诊断：glm 有效批次 0/77 → **67/77（87%）**；qwen 0/77 → **41/77（53%）**（剩余主要为 quote 改写，属模型保真度）。
- 端到端试点（真实 runner，2 题）：failedExtractions 6/题 → 2–3/题，**证据召回 0 → 1**，答案上下文接近 6,000 字符上限。

**v2 全量运行**：glm-5.3-flash 作答、qwen3.8-flash 替代裁判，并发 8，证据目录 `artifacts/memory-evaluation-glm-qwen-v2-20260922/`。运行完成前不报告准确率；完成后将与 v1 的 10.0% 直接对比，以分离评测器缺陷与产品/模型因素。

环境记录：glm 上游强制思考（`thinking:{type:"disabled"}` 与 `reasoning_effort:"none"` 均被 400 拒绝），通过隔离 agent 目录的模型映射改发 `reasoning_effort:"low"` 解决，未改动产品代码；本机 `~/.pi/agent` 未动。judge 的 Python 调用复用 Pix 托管运行时（复制为 `python3.exe`）；官方 `evaluate_qa.py` 经镜像下载，SHA-256 与 pinned 摘要一致。

## 多平台执行

原验收主机为 macOS ARM64；2026-09-22 起新增 Windows x64 主机（i7-12700 / Node 24.20.0），LongMemEval-S 全量评测已在后者完成。GitHub 仓库 `wyxplus/pix` 是公开仓库，源码已在 `main` 上，并已发布 `v0.8.1`。四平台验收工作流改成纯手动触发：dispatch 时可以填写 `ref`（分支、标签或提交），留空则取发起本次运行的分支或标签，因此不再需要专门的测试分支，也不会在推送时自动消耗四台 runner。工作流只构建和上传验收产物，不创建 Release；Windows 另外检查原生 WebView 中的记忆开关、持久化与遗忘。

本轮尚未运行该工作流，macOS Intel / Windows x64 / Linux x64 的安装包记忆验收仍为“尚未运行”。

Apple 发布证书/公证凭据目前不可用。即便四个平台的临时构建全部通过，也不能据此宣称已通过发布签名或公证验收。
