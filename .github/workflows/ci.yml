name: CI
on:
  push:
    branches: [main]
  pull_request:
  workflow_call:
concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  windows-powershell:
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.15.1
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Verify PowerShell execution and platform tool lifecycle
        run: pnpm --filter @pix/agent-runtime exec vp test --run test/platform-tools.test.ts
      - name: Verify Windows paths, Node CLI shims and external terminal launch
        run: pnpm --filter @pix/desktop exec vp test --run --config vite.renderer.config.ts ../main/shell-path.test.ts ../main/node-cli-launch.test.ts ../main/windows-terminal.test.ts
      - name: Verify configuration persistence and worktree data protection on Windows
        run: |
          pnpm --filter @pix/agent-runtime exec vp test --run test/auth-json.test.ts test/models-json.test.ts test/provider-base-url.test.ts
          if ($LASTEXITCODE -ne 0) { throw 'Configuration regression failed' }
          pnpm --filter @pix/contracts exec vp test --run test/path-key.test.ts
          if ($LASTEXITCODE -ne 0) { throw 'Path regression failed' }
          pnpm --filter @pix/desktop exec vp test --run --config vite.renderer.config.ts ../main/worktree-prune.test.ts ../main/host-park-policy.test.ts ../sidecar/transport.test.ts
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.15.1
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri
      - name: Tauri system libraries
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
      - run: pnpm build
      - run: pnpm test
      - run: pnpm check:rust
      - run: pnpm --filter @pix/desktop exec node scripts/sidecar-smoke.test.mjs
      - run: pnpm test:release-assets
      - run: pnpm test:release-version
      - name: Install browser test engines
        run: pnpm --filter @pix/desktop exec playwright install --with-deps chromium webkit
      - name: Browser and Sidecar regression
        run: pnpm --filter @pix/desktop exec playwright test
